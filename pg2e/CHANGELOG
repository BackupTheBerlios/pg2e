-main.pl: All $scrollbar->add_with_viewport changed to $scrollbar->add resolving scroll problems. Window title changes dinamically when a notebook's page is selected. When saving a page the tab_label changes to the filename, if it isn't already the filename itself.
